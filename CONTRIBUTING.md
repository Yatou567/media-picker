# 问题反馈与开发

## 报告问题

请先确认问题是否属于 README 中的已知限制。提交 Issue 时建议包含：

- Chrome 版本、操作系统、插件版本。
- 操作步骤、预期结果与实际结果。
- 可公开访问的复现网页，或不包含私人信息的简化示例。
- 脱敏后的错误提示或截图。

请勿公开 Cookie、访问令牌、含签名参数的私人链接、未获授权的媒体内容。

## 本地检查

需要 Node.js；无需安装 npm 依赖。在仓库根目录运行：

```sh
node tests/run.cjs
node --check background.js
node --check content.js
node --check popup.js
```

同时应在真实 Chrome 中验证：加载插件、分类与搜索、单项下载、批量下载、点选与 Esc 退出、页面动态更新、空结果及不支持的页面。离线测试不覆盖浏览器权限和真实网络下载。

本项目尚未指定开源许可证；如需修改并分发，请先与仓库所有者确认授权。
