# 版本记录

## v1.0.0 · 首个预览版本

### 功能

- 识别图片、视频、音频、背景图、常见懒加载和响应式图片。
- 按完整 URL 去重，保留查询参数和签名。
- 分类、关键词搜索、单项另存为与批量下载（每次最多 100 项）。
- 点选网页元素，列出其中的媒体，按 Esc 退出。
- 支持可访问的同源 iframe 和开放 Shadow DOM。
- 弹窗打开期间自动检查资源更新，显示本次会话的下载状态。

### 验证与边界

16 项离线测试通过；这些测试使用 DOM 与 Chrome API 模拟。真实 Chrome 安装、布局、点选与网络下载仍待验证，因此本次标记为预发布。

暂不支持 DRM、HLS/DASH 合并、blob 临时资源、跨域框架内部内容，以及 Chrome 内部页或本地文件页面。下载成功与否也取决于网站鉴权及防盗链设置。
